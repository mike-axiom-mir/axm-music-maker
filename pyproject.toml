[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "axm-music-maker"
version = "0.1.0"
description = "Structured, editable adaptive music state for AXM"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["axm_music*"]
